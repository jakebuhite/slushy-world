class ProductModel {
    constructor(id, name, description, img, price, isNew) {
      this.id = id
      this.name = name
      this.description = description
      this.img = img
      this.price = price
      this.isNew = isNew
    }
  }
  
export default ProductModel